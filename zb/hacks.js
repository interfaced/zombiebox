/* eslint-disable */

// Workaround for https://github.com/google/closure-compiler/issues/3139
// TODO: Remove once issue is resolved
window[Symbol('Object spread Object.assign polyfill hack')] = Object.assign({}, {});
